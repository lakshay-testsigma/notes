---
title: "Git"
domain: "git"
summary: "Eighteen recap notes, in course order — from first commit to what is actually inside .git."
tags: ["git", "github", "version-control"]
difficulty: "beginner"
updated: "2026-07-27"
icon: "🌿"
---

<Diagram caption="Read top to bottom — each note assumes the one before it." chart={`
flowchart LR
  A[Foundations] --> B[Local work] --> C[Branch & merge] --> D[Undo] --> E[GitHub] --> F[Teamwork] --> G[Internals]
`} />

Every note is a **recap**: step-through visuals first, commands in copyable blocks,
detail in tables. Click any checkpoint or step inside a visual to replay it.
